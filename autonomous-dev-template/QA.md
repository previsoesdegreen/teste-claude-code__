# QA.md — Registro de Testes (compacto)

> Uma entrada por incremento, nesta ordem cronológica (mais recente no final). Não repetir descrição do item (já está no `BACKLOG.md`) nem estado atual (já está no `STATE.md`). Revisão de segurança completa e Release Checklist só acontecem no fechamento de fase/release — registrar isso aqui como uma entrada própria quando ocorrer, não a cada item.

## Formato de cada entrada
```
ID:
Testes executados:
Resultado:
Bugs encontrados:
Correções:
Regressão:
Validação no navegador:
Observações:
```

## Entradas

### BKL-000 (exemplo — remover ao usar)
```
ID: BKL-000
Testes executados: manual — fluxo X
Resultado: OK
Bugs encontrados: nenhum
Correções: —
Regressão: OK (fluxos Y e Z revalidados)
Validação no navegador: OK, sem erros de console
Observações: —
```
